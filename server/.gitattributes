# 统一用 LF，避免 Windows/Linux 之间来回换行
* text=auto eol=lf

# 二进制文件不要做换行/编码转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.zip binary
*.apk binary
*.jar binary
*.keystore binary
*.jks binary
*.dex binary

# 这些是给 git 看的，别做 diff
*.jsonl -diff
